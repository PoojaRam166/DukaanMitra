# Local dev: leave unset, api.ts falls back to http://localhost:5000/api
# Production: set this in your host's env vars (Vercel/Netlify) to your
# deployed backend's URL, e.g. https://your-backend.onrender.com/api
VITE_API_URL=
